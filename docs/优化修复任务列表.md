# AML 系统优化修复任务列表

> 基于源码分析报告评估，结合实际代码验证
> 生成日期：2026-05-04
> 原则：可落地、按优先级、每项可独立交付

---

## 阶段一：安全与合规（阻塞投产）

### S1-1 前端 RBAC 权限控制 `🔴 最高`
- [ ] 后端：`/api/auth/me` 返回用户角色和权限列表
- [ ] 前端：创建 `usePermission` hook，提供 `hasPermission(code)` 方法
- [ ] 前端：路由守卫校验角色，无权限跳转 403 页面
- [ ] 前端：侧边栏菜单根据权限动态过滤
- [ ] 前端：按钮级权限指令 `v-perm="'customer:create'"`
- [ ] 前端：创建 403 无权限页面
- 预估：8h

### S1-2 CORS 生产环境配置 `🔴 最高`
- [ ] `SecurityConfig.java`：prod profile 限制具体域名
- [ ] `WebMvcConfig.java`：prod profile 移除 `allowCredentials(true)` 或限制 origin
- [ ] 验证 dev/test 环境不受影响
- 预估：1h

### S1-3 Kafka 事务一致性 `🔴 高`
- [ ] `TransactionServiceImpl`：改用 `TransactionalEventListener(phase=AFTER_COMMIT)` 发送 Kafka
- [ ] 事务回滚时不发送消息
- [ ] 添加单元测试验证事务回滚场景
- 预估：3h

### S1-4 ECharts 内存泄漏修复 `🔴 高`
- [ ] `DashboardView.vue`：`onUnmounted` 中调用 `chart.dispose()`
- [ ] `DashboardView.vue`：`onUnmounted` 中 `removeEventListener('resize', ...)`
- [ ] 提取 `useChart` composable 统一管理 ECharts 生命周期
- 预估：2h

### S1-5 敏感配置外部化 `🟡 中`
- [ ] `application-dev.yml`：保持现状（开发环境合理）
- [ ] `application-prod.yml`：确认所有敏感值已用环境变量占位符
- [ ] 添加 `.env.example` 文档说明所需环境变量
- [ ] 启动脚本 `start.sh` 增加环境变量检查
- 预估：2h

**阶段一小计：16h**

---

## 阶段二：性能优化（影响用户体验）

### S2-1 筛查服务 N+1 查询优化 `🔴 高`
- [ ] `ScreeningServiceImpl`：改为批量查询白名单 + 内存匹配
- [ ] 制裁名单增加 Redis 缓存（`@Cacheable`）
- [ ] 添加缓存失效机制（名单更新时清除缓存）
- 预估：4h

### S2-2 分页查询 N+1 优化 `🟡 中`
- [ ] `AlertServiceImpl.pageQueryAlerts`：批量查询规则明细，内存组装
- [ ] `CaseServiceImpl.pageQueryCases`：批量查询调查记录数，内存组装
- [ ] 使用 `MyBatis-Plus` 的 `selectBatchIds` 或自定义 SQL 关联查询
- 预估：3h

### S2-3 Redis 缓存接入 `🟡 中`
- [ ] `CustomerService`：客户详情 `@Cacheable("customer:#id")`
- [ ] `DictService`：字典数据 `@Cacheable("dict:#code")`
- [ ] `ScreeningService`：白名单 `@Cacheable("whitelist")`
- [ ] 各缓存设置合理 TTL（客户 10min，字典 30min，白名单 5min）
- 预估：4h

### S2-4 前端 401 跳转优化 `🟢 低`
- [ ] `request.ts`：`window.location.href` 改为 `router.push('/login')`
- [ ] 避免整页刷新，保持 SPA 体验
- 预估：0.5h

**阶段二小计：11.5h**

---

## 阶段三：代码质量（可维护性）

### S3-1 TypeScript 类型定义 `🟡 中`
- [ ] 创建 `src/types/api.d.ts`：定义所有 API 响应接口
- [ ] 创建 `src/types/models.d.ts`：定义业务实体接口（Customer, Alert, Case 等）
- [ ] 创建 `src/types/common.d.ts`：PageResult, PageQuery 等通用类型
- [ ] 逐页面替换 `any` 为具体类型（优先核心页面）
- 预估：6h

### S3-2 提取前端公共组件 `🟡 中`
- [ ] `StatusTag.vue`：统一状态标签组件（传入 status + type map）
- [ ] `PageTable.vue`：封装分页表格（loading/data/pagination/slots）
- [ ] `SearchForm.vue`：封装搜索表单 + 重置逻辑
- [ ] `usePagination` composable：分页状态管理
- [ ] 逐页面迁移使用新组件
- 预估：8h

### S3-3 API 封装统一 `🟡 中`
- [ ] `api/modules.ts`：补全所有模块的 API 封装
- [ ] 逐页面将 `request.get('/hard-coded/path')` 替换为 `api.xxx.method()`
- [ ] 统一错误处理和 loading 状态
- 预估：4h

### S3-4 构造器注入统一 `🟢 低`
- [ ] 将剩余 39 处 `@Autowired` 字段注入改为 `@RequiredArgsConstructor`
- [ ] 逐模块批量替换，编译验证
- 预估：2h

### S3-5 循环依赖解除 `🟢 低`
- [ ] 分析 2 处 `@Lazy` 循环依赖的根因
- [ ] 通过事件驱动或接口抽象解除耦合
- 预估：2h

**阶段三小计：22h**

---

## 阶段四：测试补充（质量保障）

### S4-1 核心模块单元测试 `🔴 高`
- [ ] `AuthServiceImpl`：登录/刷新/登出/密码校验
- [ ] `TransactionServiceImpl`：交易入库/Kafka 发送
- [ ] `RuleEngineService`：四引擎并行评估
- [ ] `CaseServiceImpl`：案件创建/状态流转
- [ ] `ScreeningServiceImpl`：单次/批量筛查
- 目标：每个 Service 至少 10 个测试用例
- 预估：16h

### S4-2 前端 E2E 测试 `🟡 中`
- [ ] 登录流程 E2E
- [ ] 客户管理 CRUD E2E
- [ ] 预警处理全流程 E2E
- 使用 Playwright 或 Cypress
- 预估：8h

**阶段四小计：24h**

---

## 阶段五：优化增强（锦上添花）

### S5-1 MapStruct 引入 `🟢 低`
- [ ] 替换 `BeanUtils.copyProperties` 为 MapStruct Mapper
- [ ] 类型安全，编译期生成，性能更好
- 预估：4h

### S5-2 状态枚举统一 `🟢 低`
- [ ] 创建统一状态枚举类（AlertStatus, CaseStatus, ReportStatus 等）
- [ ] 替换代码中的 Magic String
- 预估：3h

### S5-3 前端 404 页面 `🟢 低`
- [ ] 创建 404 NotFound 页面
- [ ] 路由配置 `path: '/:pathMatch(.*)*'` 兜底
- 预估：1h

### S5-4 图标按需引入 `🟢 低`
- [ ] `vite-plugin-icons` 替代全量引入 Element Plus Icons
- [ ] 减少打包体积
- 预估：2h

**阶段五小计：10h**

---

## 总览

| 阶段 | 内容 | 工时 | 优先级 |
|------|------|------|--------|
| 阶段一 | 安全与合规 | 16h | 🔴 阻塞投产 |
| 阶段二 | 性能优化 | 11.5h | 🟡 影响体验 |
| 阶段三 | 代码质量 | 22h | 🟡 可维护性 |
| 阶段四 | 测试补充 | 24h | 🟡 质量保障 |
| 阶段五 | 优化增强 | 10h | 🟢 锦上添花 |
| **合计** | | **83.5h** | |

---

## 执行建议

1. **阶段一** 必须在投产前完成（S1-1 权限控制最关键）
2. **阶段二** 可与阶段一并行，性能问题随数据量增长会放大
3. **阶段三** 持续迭代，不必一次性完成
4. **阶段四** 优先覆盖核心流程（登录→交易→预警→案件→报送）
5. **阶段五** 排期灵活，有空闲时穿插进行

---

*本列表基于实际代码验证，已排除分析报告中夸大或不准确的项*
