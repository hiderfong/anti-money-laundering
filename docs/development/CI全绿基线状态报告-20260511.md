# CI 全绿基线状态报告

> 日期：2026-05-11
> 基线名称：`v0.1.0-ci-green`
> 项目路径：`/Users/nathan/Work/Anti-money-Laundering`

---

## 一、结论

当前项目已完成 GitHub 私有仓库接管、完整 Git 历史推送、GitHub Actions 云端质量门验证，并已固化为 `v0.1.0-ci-green` 基线。

本基线可作为后续完善、部署准备和外部测试的参照点。

---

## 二、基线信息

| 项目 | 内容 |
|------|------|
| GitHub 仓库 | `https://github.com/hiderfong/anti-money-laundering` |
| 仓库可见性 | 私有 |
| 主分支 | `main` |
| 基线 tag | `v0.1.0-ci-green` |
| 基线 commit | `9d90f354ce1be8203b0295639f32fd08d8464815` |
| commit message | `test: keep rbac e2e product code short` |
| GitHub Release | `https://github.com/hiderfong/anti-money-laundering/releases/tag/v0.1.0-ci-green` |
| GitHub Actions run | `https://github.com/hiderfong/anti-money-laundering/actions/runs/25652700780` |
| 原 GitHub 快照备份分支 | `github-snapshot-20260511-134740` |

---

## 三、GitHub Actions 结果

GitHub Actions run `#4` 结果：全部通过。

| Job | 结果 |
|-----|------|
| `Backend Unit And Integration Tests` | PASS |
| `Frontend Build` | PASS |
| `Production Readiness Checks` | PASS |
| `End-To-End Release Gate` | PASS |
| `Container Build` | PASS |

其中 `End-To-End Release Gate` 覆盖：

| 脚本 | 覆盖内容 |
|------|----------|
| `scripts/e2e-test.sh` | 后端 API 端到端主流程 |
| `scripts/frontend-e2e.sh` | 前端 smoke、Vite 代理、核心 API 与限流 |
| `scripts/frontend-browser-e2e.sh` | Playwright 浏览器登录、路由、控制台错误检查 |
| `scripts/prepare-rbac-e2e-users.sh --execute` | RBAC 测试账号准备 |
| `scripts/rbac-e2e.sh` | 四角色权限、403 越权拦截、允许路径 |

---

## 四、已完成的关键修正

| 类别 | 内容 |
|------|------|
| GitHub 仓库 | 创建 `hiderfong/anti-money-laundering` 私有仓库 |
| GitHub 历史 | 将 GitHub `main` 从 API 上传快照切换为本地完整 Git 历史 |
| GitHub 快照备份 | 将原 API 快照保存到 `github-snapshot-20260511-134740` |
| GitHub Actions MySQL | 为 GitHub hosted runner 显式映射 MySQL service 端口，并使用 `127.0.0.1` 连接 |
| RBAC E2E 数据 | 缩短产品编码生成逻辑，避免 GitHub run id 较长时超过 `t_product.product_code VARCHAR(32)` |
| 基线版本 | 创建并推送 `v0.1.0-ci-green` tag |
| Release | 创建 GitHub prerelease，记录 run #4 全绿基线 |
| Git 凭据 | 使用 macOS Keychain 保存 GitHub HTTPS token，remote URL 不写 token |

---

## 五、当前限制

| 限制 | 说明 | 建议 |
|------|------|------|
| 私有仓库分支保护 | GitHub 免费账户下私有仓库不能启用 branch protection | 保持私有；后续公开或升级 GitHub Pro 后再启用 |
| `main` 直接推送 | 当前无法从 GitHub 侧强制阻止直接 push/force push | 操作上保持先测试、后推送；关键节点打 tag |
| CI 报告结构化 | 当前 Actions 已上传部分 artifact，但 E2E 汇总仍主要依赖日志 | 下一阶段增加结构化 `e2e-summary.md` 与日志归档 |
| 生产部署 | 当前已具备 readiness check 和 Docker 构建，但仍需真实生产环境变量与部署演练 | 下一阶段完善部署手册与沙箱外部署验证 |

---

## 六、后续建议

优先级建议如下：

| 优先级 | 工作项 | 目标 |
|--------|--------|------|
| P0 | CI E2E 报告自动归档 | 每次 Actions 生成可下载的结构化测试报告 |
| P0 | 生产部署准备 | 完善 `.env.example`、部署脚本、Docker Compose 与 readiness check |
| P1 | 前端权限体验细化 | 优化无权限菜单、403 页面、登录过期刷新、角色展示 |
| P1 | 安全基线强化 | 增加 secret scanning 指引、默认密码风险提示、生产密钥检查 |
| P2 | GitHub 分支保护 | 公开仓库或升级 GitHub Pro 后启用 required checks |

建议下一步先做 P0 的 `CI E2E 报告自动归档`，原因是：

- 当前 E2E 已经稳定跑通。
- 报告归档能提升每轮测试可审计性。
- 后续外部 Agent、GitHub Actions、本地 Gitea 结果可以统一对齐。

---

## 七、验收命令

查看当前基线 tag：

```bash
git show --no-patch --format=fuller v0.1.0-ci-green
```

验证 GitHub `main`：

```bash
GIT_TERMINAL_PROMPT=0 git ls-remote github refs/heads/main
```

查看 GitHub Actions：

```text
https://github.com/hiderfong/anti-money-laundering/actions
```

查看 GitHub Release：

```text
https://github.com/hiderfong/anti-money-laundering/releases/tag/v0.1.0-ci-green
```
