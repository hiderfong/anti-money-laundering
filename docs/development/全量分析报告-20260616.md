# 反洗钱系统 — 全量分析报告

> 生成日期：2026-06-16
> 代码基线：`8e464ad` (main)，与 `github/main` 同步，工作树干净
> 构建状态：`mvn test` 全绿 — **269 tests / 0 failures / 0 errors**
> 对照基线：`docs/development/全量分析报告-20260530.md`
> 分析范围：后端 Java、前端、测试、安全、配置、数据库迁移、文档与仓库卫生

---

## 一、项目概览（当前快照 vs 上次全量报告）

| 维度 | 当前 (6-16) | 上次 (5-30) | 变化 |
|------|------|------|------|
| 后端 Java 文件 / LOC | 371 / 33,746 | 371 / 33,731 | ≈持平 |
| 后端测试文件 / `@Test` | 42 / 271 | 40 / 238 | ↑ +2 文件 / +33 用例 |
| 测试通过 | 269 / 0 / 0 | 236 / 0 / 0 | ↑ +33（全绿） |
| 前端 Vue/TS 文件 / LOC | 51 / 19,499 | 51 / 19,499 | 持平 |
| 前端测试 | **0** | 0 | 未变 |
| REST 控制器 | 27 | 27 | — |
| Flyway 迁移 | V001–V016 | V001–V016 | — |
| TODO/FIXME | 2 | 2 | — |

技术栈未变：Java 21 / Spring Boot 3.4.1 / MyBatis-Plus / Spring Security + JWT；前端 Vue 3 + TS + Vite + Element Plus + Pinia；中间件 MySQL+Redis+Kafka+ES+Neo4j+MinIO；ML 用 Smile（IsolationForest + 逻辑回归）。

**自上次全量报告以来的实质改动**仅两块（均为本系列工作）：P1-A 安全授权修复（`7d932aa`）、P1-B STR/CTR 报送测试覆盖（6 个提交）。生产代码主体未变（+15 LOC 系 AI 编排器 lint 触动）。

---

## 二、上次报告问题的闭环情况

| 编号 | 问题 | 状态 |
|------|------|------|
| **P1-A** | 审计日志/监控端点缺授权 | ✅ **已修复**（`7d932aa`）：AuditLogController、RuleFeedbackController、GraphAnalysisController 各补 4 个 `@PreAuthorize`，对齐 RBAC 矩阵（system:view / monitoring:view）；NotificationController 按矩阵"登录即可"正确豁免。配 4 个 RBAC 集成测试断言 403 |
| **P1-B** | 监管报送/合规模块零单测 | ◑ **部分完成**：STR + CTR 报送状态机已覆盖（+29 用例，`StrReportServiceImplTest` 15、`LargeTxnReportServiceImplTest` 14，含合规关键的"未复核不可上报"门禁断言）。assessment / prevention / rectification **仍零测试**（按范围决策推迟） |
| P1-C | 前端零测试 | ❌ 未动（19,499 行，仍仅 1 个 Playwright e2e 脚本） |
| P2-A | 默认 profile `/actuator/env` 无认证可达 | ❌ 未动（`application.yml` 仍 `include: health,info,metrics,env`；prod 已收敛） |
| P2-B | 3 个重复死视图 | ❌ 未动（`alerts/AlertListView.vue`、`cases/CaseListView.vue`、`products/ProductView.vue` 仍在，路由未引用） |
| P2-C | 三大单体服务 | ❌ 未动（RuleEngineService 1011、AlertServiceImpl 979、CustomerServiceImpl 972 行） |
| P2-D | 训练拉取无 cap | ❌ 未动（`AiRiskModelTrainingService:91` TODO(perf) 仍在；全量拉取已复核记录） |

---

## 三、当前主要问题（按优先级）

### P1 — 应优先处理

| 编号 | 问题 | 证据 | 影响 |
|------|------|------|------|
| **P1-B（续）** | 合规模块仍零单测 | assessment（2 impl）、prevention（`SpecialPreventionServiceImpl` 454 行）、rectification 零测试 | 自评估/特殊防控/整改逻辑缺回归保护（次于已覆盖的对外报送，但仍是合规链路） |
| **P1-C** | 前端零测试 | 51 文件 19,499 行无单元/组件测试 | 权限路由守卫、表单校验、i18n 回归全靠手测 |

> **已排查排除 — 原"P1-A′ 仪表盘授权"为误报**：进一步逐行核查 `DashboardController` 后确认：(1) 3 个 `/export/*` 端点全部位于 `/* ... */` 注释块内（line 81–106，TODO 待补 `listAll()`），**运行时不存在**，无数据导出暴露；(2) 仅剩的 5 个统计/概览端点，RBAC 矩阵明确规定"仪表盘 `/dashboard`：登录即可"（全角色），0 个 `@PreAuthorize` **属设计如此**（与 NotificationController 同理）。先前 grep `@GetMapping` 计数把注释块内的 3 个映射也算入，导致虚报。**结论：DashboardController 无授权缺口，不应改动**（加限制反而违背矩阵、可能锁死 viewer 角色的首页）。

### P2 — 应安排处理

| 编号 | 问题 | 说明 |
|------|------|------|
| **P2-A** | 默认 `application.yml` 暴露 `/actuator/env` 且 actuator 在 `permitAll` | dev/默认 profile 下 `GET /actuator/env` 可无认证 dump 配置；prod 已收敛。建议基线即移除 env、actuator 仅放行 health |
| **P2-B** | 3 个未路由死视图 | 删除并确认无残余引用 |
| **P2-C** | 三大单体服务未拆 | 可循 `AiRiskScoringServiceImpl`（1160→388+协作组件）范式 |
| **P2-D** | 监督模型训练拉取无 cap | 镜像已加固的 `supervised-sample-cap` 模式，补 `ORDER BY reviewed_at DESC LIMIT N` |
| **P2-E（观察）** | 更广的 RBAC 粒度不均 | 多个控制器存在"部分端点有 `@PreAuthorize`、部分无"（如 GraphController 5/1、TransactionController 3/1、UserController 7/1）。需逐一核对是否为类级注解/有意公开，避免误判；建议一次性按 RBAC 矩阵审计全部控制器 |

### P3 — 文档与收尾

- 文档版本治理：`docs/development/` 报告仍按日期堆叠，无单一"现行状态"索引页。
- AI 低优先 backlog：P1-2 FU2（`aml.ml.drift.bins` 死配置）、FU3（集成测试 `@Order` 编号）。
- P1-B 整合测试 follow-up：`retryFailedSubmissions` 的 `lt(retryCount,3)` DB 查询过滤无法单测，待 `@SpringBootTest` 切片覆盖。

---

## 四、改进建议（按优先级）

1. **近期（P2-E）**：按 `RBAC角色权限矩阵.md` 一次性审计全部 27 个控制器的端点授权，消除粒度不均（区分有意公开 vs 遗漏）。**注意**：本次的"仪表盘授权"误报教训表明，控制器授权审计必须逐行读源码（排除注释块内端点、对照矩阵的"登录即可"豁免项），不能仅凭 `@GetMapping` 计数。
2. **近期（P1-B 续）**：为 assessment / prevention / rectification 补关键路径单测。
3. **规划（P1-C）**：引入前端组件测试（Vitest + Vue Test Utils），优先覆盖路由守卫与表单校验。
4. **规划（P2-A/C/D）**：actuator env 收敛、单体拆分、训练拉取 cap。
5. **清理（P2-B）**：删除 3 个死视图。

---

## 五、总体评价

工程持续向好。上次报告最尖锐项 **P1-A（审计日志授权）已修复**并配测试；监管对外报送（STR/CTR）已建立回归安全网（+29 用例，确认两条上报路径合规门禁正确）。全量 269 测试全绿，主链路无阻断缺陷。

> 本轮一度怀疑 `DashboardController` 存在同型授权缺口，但逐行核查后**排除为误报**（导出端点为注释死代码、统计端点按矩阵"登录即可"豁免，详见 §三）。这也提示：控制器授权审计须读源码，不能仅凭 `@GetMapping` 计数——已写入 §四建议 1。

短板仍集中在**横向一致性**：①测试深度模块间不均（AI/监控/对外报送扎实，内部合规模块 assessment/prevention/rectification 与整个前端仍为零）；②个别配置（默认 profile 的 actuator env 暴露）与死代码（3 个重复视图）收尾未做；③RBAC 粒度宜按矩阵做一次全控制器审计（区分有意公开 vs 遗漏）。

**当前最值得优先的是 P1-B 续（内部合规模块单测）与 P2-A（actuator env 收敛）**——前者补齐合规链路回归保护，后者消除 dev profile 的配置泄露面。

---

## 附：分析口径

- 规模/测试数：`find`+`wc`+`grep '@Test'` 实测；测试结果：`mvn test` 全量执行，surefire 聚合 269/0/0。
- 安全：`SecurityConfig`（`anyRequest().authenticated()` + PERMIT_ALL_PATHS）+ 各控制器 `@PreAuthorize` 计数交叉核对；DashboardController 经逐行确认无类级/方法级注解。
- 闭环核实：对 P1-A 三控制器（各 4 个 @PreAuthorize）、P1-B 测试文件、P2-A/B/D 逐一复核。
