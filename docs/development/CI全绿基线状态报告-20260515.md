# CI 全绿基线状态报告

> 日期：2026-05-15  
> 基线名称：`v0.2.0-pre-release-green`  
> 项目路径：`/Users/nathan/Work/Anti-money-Laundering`

---

## 一、结论

当前项目已在 GitHub Actions 云端完成最新一轮发布门禁验证，并固化为 `v0.2.0-pre-release-green` 预发布全绿基线。

该基线相比 `v0.1.0-ci-green`，新增并验证了特别预防、调查协查、整改中心、E2E 业务种子、E2E 清理、登录权限响应修复，以及整改任务审计字段迁移。

---

## 二、基线信息

| 项目 | 内容 |
|------|------|
| GitHub 仓库 | `https://github.com/hiderfong/anti-money-laundering` |
| 仓库可见性 | 私有 |
| 主分支 | `main` |
| 基线 tag | `v0.2.0-pre-release-green` |
| 基线 commit | `f87dd631280aebe6b145a78cb5573ee158418b3a` |
| commit message | `fix: add rectification audit columns migration` |
| GitHub Release | `https://github.com/hiderfong/anti-money-laundering/releases/tag/v0.2.0-pre-release-green` |
| GitHub Actions run | `https://github.com/hiderfong/anti-money-laundering/actions/runs/25870423647` |

---

## 三、GitHub Actions 结果

GitHub Actions run `25870423647` 结果：全部通过。

| Job | 结果 |
|-----|------|
| `Production Readiness Checks` | PASS |
| `Frontend Build` | PASS |
| `Backend Unit And Integration Tests` | PASS |
| `End-To-End Release Gate` | PASS |
| `Container Build` | PASS |

其中 `End-To-End Release Gate` 覆盖：

| 步骤 | 结果 | 覆盖内容 |
|------|------|----------|
| `Prepare RBAC E2E users` | PASS | 多角色 E2E 用户准备 |
| `Run API E2E` | PASS | 登录、KYC、交易、筛查、特别预防、调查协查、整改中心、报送、系统 API |
| `Run frontend smoke E2E` | PASS | 前端代理、核心页面 smoke、限流 |
| `Run frontend browser E2E` | PASS | Playwright 浏览器登录、路由、控制台错误检查 |
| `Run RBAC E2E` | PASS | 四角色权限、403 越权拦截、允许路径 |

---

## 四、本基线新增关键修正

| 类别 | 内容 |
|------|------|
| 功能闭环 | 增加特别预防、调查协查、整改中心接口与前端入口 |
| 数据迁移 | 新增 `V007__special_prevention_and_rectification.sql`、`V008__investigation_assistance.sql`、`V009__add_rectification_audit_columns.sql` |
| 测试数据 | 增加 `seed-e2e-business-data.sh`，覆盖 18 类业务闭环数据 |
| 数据清理 | 增加并修复 `cleanup-e2e-data.sh`，支持 E2E 数据可识别、可清理 |
| 权限认证 | 登录接口和 `/auth/me` 返回 `roles`、`permissions`；E2E 凭据与种子管理员密码对齐 |
| CI 稳定性 | 修复 GitHub Actions API E2E 登录失败和整改任务创建 500 |
| 前端体验 | 优化操作按钮、菜单尺寸、固定列背景遮挡等可视问题 |
| 发布门禁 | 最新 GitHub Actions 发布门禁全绿，容器镜像构建通过 |

---

## 五、后续处理状态更新

| 优先级 | 工作项 | 说明 |
|--------|--------|------|
| P0 | V009 升级部署验证 | 已由外部 Agent 在真实 Docker/MySQL 环境验证通过，报告见 `docs/development/V009升级部署验证报告-20260515.md` |
| P1 | 反馈接口契约修复 | 已完成，前端 `feedbackApi` 已统一到 `/monitoring/rules/feedback/*` |
| P1 | 监管报送真实闭环 | 已完成内部 XML、提交响应和提交日志闭环；真实监管平台直连接口作为后续外部集成项 |
| P1 | 预警分配人员查询 | 已完成，改为根据真实角色查询可分配人员 |
| P2 | 图分析产品化 | 已补 API 类型、可选 E2E 和验收说明；后续可继续做性能基线和页面证据 |
| P2 | 生产化增强 | 已补请求追踪；统一错误码文档、审计覆盖率、性能基线仍可继续加强 |

---

## 六、验收命令

查看当前基线 tag：

```bash
git show --no-patch --format=fuller v0.2.0-pre-release-green
```

验证 GitHub `main`：

```bash
GIT_TERMINAL_PROMPT=0 git ls-remote github refs/heads/main
```

查看 GitHub Actions：

```text
https://github.com/hiderfong/anti-money-laundering/actions/runs/25870423647
```

查看 GitHub Release：

```text
https://github.com/hiderfong/anti-money-laundering/releases/tag/v0.2.0-pre-release-green
```
