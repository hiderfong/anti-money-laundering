# 全量分析报告 20260530 当前分支复核

> 复核日期：2026-05-30
> 当前分支：`codex/ai-risk-follow-up-tasks`
> 当前 HEAD：`7c1d60c`
> 远端报告提交：`github/main` `87859da`
> 复核对象：`docs/development/全量分析报告-20260530.md`

## 结论

`全量分析报告-20260530.md` 对其标注基线 `3b30475/main` 的判断总体合理，可以作为当时主干的有效问题清单。

但当前工作分支已经包含多项后续治理改动，因此不能直接照单全收。应将该报告转化为当前分支适用清单：已修复项关闭，仍有效项继续推进，需要复核项单独验证。

## 已在当前分支解决或基本解决

| 报告项 | 当前状态 | 复核依据 |
|--------|----------|----------|
| `AuditLogController` 缺少授权 | 已解决 | 当前类级别已有 `@PreAuthorize("hasRole('ADMIN') or hasAuthority('system:view')")` |
| `RuleFeedbackController` 缺少授权 | 已解决 | 当前类级别已有 `@PreAuthorize("hasRole('ADMIN') or hasAuthority('monitoring:view')")` |
| `GraphAnalysisController` 缺少授权 | 已解决 | 当前类级别已有 `@PreAuthorize("hasRole('ADMIN') or hasAuthority('monitoring:view')")` |
| AI 监督模型训练样本拉取无 cap | 已解决 | `AiRiskModelTrainingService` 已按 `reviewedAt` 降序、训练窗口和 `trainingSampleCap` 限制样本 |

## 仍然有效，应继续推进

| 优先级 | 事项 | 当前判断 | 建议动作 |
|--------|------|----------|----------|
| P1 | 默认 actuator 暴露过宽 | `application.yml` 仍包含 `health,info,metrics,env`，`SecurityConfig` 仍 permitAll `/actuator/**` | 默认配置移除 `env`；安全配置仅放行 `/actuator/health`，其余 actuator 要求认证 |
| P1 | 监管报送、风险自评估、特殊防控、整改链路测试不足 | `src/test/java` 下未发现 reporting/assessment/prevention/rectification 专项测试目录或关键路径测试 | 优先补 STR/CTR 报送、审批、自评估评分、整改状态流转测试 |
| P1 | 前端组件/单元测试缺失 | `frontend/package.json` 无 Vitest/Vue Test Utils，只有 `e2e:browser` 脚本 | 引入 Vitest + Vue Test Utils，先覆盖权限路由、登录态、关键表单校验 |
| P2 | 前端重复死视图 | 路由仍引用 `views/alert`、`views/case`、`views/product`，而 `views/alerts`、`views/cases`、`views/products` 未被路由使用 | 删除死视图或明确迁移路由到新视图，避免重复维护 |
| P2 | 三大核心服务仍偏大 | 当前 `RuleEngineService` 1011 行、`AlertServiceImpl` 979 行、`CustomerServiceImpl` 972 行 | 参考 AI 子系统拆分方式，按规则评估、预警编排、客户画像/风险/详情组装拆分 |
| P3 | 文档版本治理不足 | `docs/development` 历史报告较多，缺少“当前有效状态”索引 | 新增 `docs/development/README.md`，标记现行、归档、外部测试报告 |

## 需要单独复核后再决定

| 事项 | 当前观察 | 处理建议 |
|------|----------|----------|
| `NotificationController` 是否必须加 `@PreAuthorize` | 该控制器接口均基于 `SecurityUtils.getCurrentUserId()` 查询或更新当前用户通知，服务层也按 `userId` 过滤；在 `anyRequest().authenticated()` 下不属于“任意读全量通知”的问题 | 可保留认证即可；若追求 RBAC 形式一致，可加 `hasRole('ADMIN') or hasAuthority('notification:view') or isAuthenticated()`，但收益有限 |
| 数据契约是否补外键 | 历史通知与案件/预警引用已通过服务层兼容补齐，但是否上 DB 外键会影响历史数据、测试数据和软删除策略 | 先做数据一致性扫描，再决定是否补约束或只补写入校验 |

## 建议执行顺序

1. 修复 actuator 默认暴露和 permitAll 范围。
2. 为 STR/CTR 报送链路补最小关键路径测试。
3. 为自评估、特殊防控、整改中心补状态流转测试。
4. 清理或合并前端死视图。
5. 引入前端 Vitest 测试基础设施，先覆盖权限路由和核心表单。
6. 分阶段拆分 `RuleEngineService`、`AlertServiceImpl`、`CustomerServiceImpl`。
7. 新增 `docs/development/README.md`，统一索引现行报告、归档报告和外部验证材料。

## 当前分支建议

下一步最适合先处理 actuator 安全基线，因为改动小、收益明确、测试成本低；随后补监管报送测试，这会直接提升合规主链路的回归保护。
