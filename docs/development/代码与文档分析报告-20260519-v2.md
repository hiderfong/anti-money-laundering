# 代码与文档分析报告 v2（增量复查）

> 生成日期：2026-05-19
> 上次基线：`8ed94e5` → 本次基线：`f5ec96e`
> 对照文档：`docs/development/代码与文档分析报告-20260519.md`
> 本次重点：自上次分析后的变更评估 + 遗留问题跟踪

---

## 一、本次变更概览（6 个新提交）

| 提交 | 内容 | 评价 |
|------|------|------|
| `f5ec96e` | feat: AI 反洗钱风险评分与人工复核工作流 | 重大功能新增 |
| `9a1049b` | fix: 健康检查响应隐藏数据库 URL | ✅ 修复信息泄露 |
| `f07bbe9` | docs: 补充简体中文代码注释 | 可维护性提升 |
| `1fb050a` | docs: 生产运维收口计划 | 文档完善 |
| `bf1c2d6` | ci: Maven 镜像失败回退中央仓库 | CI 健壮性提升 |
| `269aa11` | chore: systemd 服务安装脚本 | 部署能力补强 |

代码规模变化：Java 文件 344 → **358**（+14）；行数 30,175 → **32,422**（+2,247）；测试文件 29 → **30**（+1）；Flyway 迁移 V013 → **V015**（新增 AI 评分记录 / 复核反馈两张表）。

---

## 二、新增功能评估：`module/ai` AI 风险评分

新增垂直模块 `com.insurance.aml.module.ai`，14 个 Java 文件，含 controller / service / mapper / entity / 9 个 DTO，配套 V014、V015 迁移及前端 4 个视图改造（AlertList、CustomerDetail、ModelManagement、Monitoring）。

**正面**：
- 严格沿用既有 `module/*` 垂直切分约定，结构一致，未引入新的架构风格。
- 配套 `AiRiskScoringIntegrationTest`（224 行）集成测试，新功能有端到端验证。
- 数据库变更走 Flyway（V014/V015），符合迁移规范。

**风险**：
- ⚠️ **`AiRiskScoringServiceImpl` 达 1,160 行**，已超过 `RuleEngineService`(1011 行) 成为**全代码库最大单文件**，单一实现类承载评分+复核+特征汇总+模型状态等多职责，圈复杂度高、可测性差。
- ⚠️ **仅有集成测试，无单元测试**：核心评分算法/因子计算逻辑缺少隔离的快速单测，回归风险集中在重型集成测试上。

---

## 三、遗留问题跟踪

| 编号 | 上次结论 | 本次状态 | 说明 |
|------|----------|----------|------|
| **P1-1** `backups/` 未忽略 + 敏感物料 | 待处理 | ❌ **未修复** | `backups/` 仍为 untracked，仍含 `admin_hash_*.txt`、`aml_system*.sql.gz`、jar；`.gitignore` 仍无 `backups/` / `._*` |
| **P1-2** 测试覆盖偏薄 | 待处理 | ❌ **未改善（相对恶化）** | 30/358 ≈ 8%；新增 1160 行最大文件无单测，核心服务（RuleEngine/Alert/Customer/AiRiskScoring）仍缺隔离单测 |
| **P1-3** 数据修复型迁移频繁 | 待处理 | ➖ 持平 | V014/V015 为新功能表，非修复型；notification 数据契约问题未见根治动作 |
| **P2-1** `case_` 命名遗留 + 空 `case` 目录 | 待处理 | ❌ **未修复** | 仍 23 文件 / 空 `case` 目录 |
| **P2-2** `service/*` 与 `module/*` 双结构 | 待处理 | ➖ 持平 | 新模块走 `module/*`（正确），但遗留双结构未收尾 |
| **P2-3** AppleDouble `._*` 污染 | 待处理 | ❌ **未修复** | `frontend/._dist`、`backups/**/._*` 仍在 |
| 信息泄露（健康检查暴露 DB URL） | 未识别 | ✅ **已修复** | `9a1049b` 已隐藏，正面进展 |

---

## 四、本轮建议（优先级排序）

1. **P1-1 仍最紧急**：本轮功能开发未触及该风险，AI 评分上线前务必先 `echo 'backups/' >> .gitignore`、加 `._*`，并核查 `git log --all -- backups/` 确认历史无误提交。
2. **拆分 `AiRiskScoringServiceImpl`**：按职责（评分计算 / 复核池 / 特征汇总 / 模型状态）拆为多个协作类或领域服务，为评分算法补隔离单元测试。
3. **测试策略升级**：从"文件数"转向"核心域行覆盖率"门禁；新增四大重型服务（RuleEngine、Alert、Customer、AiRiskScoring）的关键路径单测。
4. **结构债收尾**：本轮新模块已证明 `module/*` 约定可行，可启动 `service/*` 迁移与 `case_` 重命名，避免债务随功能增长扩大。

---

## 五、总体评价（v2）

本轮迭代质量**整体正向**：新功能严格遵循既有架构约定、带集成测试、修复了一个信息泄露问题、补强了 CI 与部署能力。但**P1 级风险（backups 仓库卫生、测试深度）连续两次未被处理**，且新增的 1160 行单体服务进一步放大了"测试深度不足"短板。建议在 AI 评分功能正式发布前，将 P1-1 与新模块单测列为发布门禁前置项。
