# 主分支集成验证报告 - 2026-06-22

## 集成范围

- 集成分支：`codex/integrate-main-ai-risk-follow-up`
- 基准分支：`github/main`
- 基准提交：`9b32b5d merge: P1-B compliance service unit tests into main`
- 合入分支：`codex/ai-risk-follow-up-tasks`
- 合入提交：`102f11a test: expand AML workflow e2e coverage`

本次集成用于收口 `github/main` 已完成的读接口权限加固、STR/CTR 及合规服务单元测试，以及当前功能分支新增的 AI 风险后续任务、模型生命周期增强、前端业务 E2E 脚本和 CI 优化。

## 冲突处理

合并过程中仅出现 1 个内容冲突：

- `src/test/java/com/insurance/aml/integration/RbacIntegrationTest.java`

处理策略：

- 保留 `github/main` 新增的读接口无权限访问 403 测试，覆盖审计日志、规则反馈、图分析、名单筛查、交易监测、自评估、STR、大额报送、产品等读接口。
- 保留功能分支新增的正向权限测试，覆盖 `system:view`、`monitoring:view` 等授权用户可正常访问对应读接口。
- 使用统一的 `MockMvcRequestBuilders.get` 静态导入，清理冲突中遗留的全限定写法。

## 集成后修正

主分支读接口权限加固合入后，两个功能分支流程测试的测试账号权限不足，导致全量后端测试首次失败：

- `AssessmentPreventionWorkflowIntegrationTest.selfAssessmentScoreCompleteAndApprove`
  - 原因：流程末尾读取评估详情需要 `assessment:view`。
  - 处理：为测试账号补充 `assessment:view`。
- `ReportingWorkflowIntegrationTest.largeTransactionReportFullWorkflow`
  - 原因：预览大额交易 XML 需要 `report:view`。
  - 处理：为测试账号补充 `report:view`。

该处理只调整测试 fixture，不放宽生产接口权限。

## 本地验证结果

已执行并通过：

- `bash scripts/secret-scan.sh`
- `bash -n scripts/secret-scan.sh scripts/prod-readiness-check.sh scripts/start-dev.sh scripts/stop-dev.sh scripts/seed-e2e-business-data.sh scripts/cleanup-e2e-data.sh scripts/customer-ui-e2e.sh scripts/monitoring-ui-e2e.sh scripts/notification-ui-e2e.sh`
- `find frontend/scripts -name '*.mjs' -exec node --check {} \;`
- `npm --prefix frontend run test:unit`
  - 2 个测试文件，5 个用例，通过。
- `npm --prefix frontend run build`
  - Vue 类型检查和 Vite 生产构建通过。
- `env JAVA_HOME=/opt/homebrew/opt/openjdk@21 mvn -q -Dtest=RbacIntegrationTest test`
  - RBAC 合并冲突定向验证通过。
- `env JAVA_HOME=/opt/homebrew/opt/openjdk@21 mvn -q -Dtest=AssessmentPreventionWorkflowIntegrationTest,ReportingWorkflowIntegrationTest test`
  - 权限 fixture 修正定向验证通过。
- `env JAVA_HOME=/opt/homebrew/opt/openjdk@21 mvn -q test`
  - 44 个测试类，348 个用例，0 failure，0 error，0 skipped。

## 远程 PR 验证补充

PR 创建后 GitHub Actions 已触发：

- Secret Scan：通过。
- Production Readiness Checks：通过。
- Frontend Build：通过。
- Backend Unit And Integration Tests：通过。
- Container Build：通过。
- End-To-End Release Gate：首次失败。

E2E 首次失败未进入业务测试阶段，失败点为 `Install Playwright Chromium headless shell`，GitHub Runner 在 `npm exec playwright install --only-shell chromium` 下载浏览器时超时并终止进程。

已追加修复：

- 在 GitHub workflow 中新增系统浏览器检测。
- 若 runner 已安装 Google Chrome，则设置 `PLAYWRIGHT_CHANNEL=chrome` 并跳过 Playwright 浏览器下载。
- 若 runner 只有 Chromium，则设置 `PLAYWRIGHT_EXECUTABLE_PATH` 并跳过下载。
- 仅当 runner 没有系统浏览器时，才回退执行 `npx playwright install-deps chromium` 与 `npx playwright install chromium`。

该修复不改变业务测试脚本，只调整 CI 浏览器准备策略，目标是降低远程 E2E 对 Playwright CDN 下载稳定性的依赖。

## 远程 E2E 第二轮修正

浏览器准备策略修复后，GitHub Actions 第二轮已进入真实业务 E2E：

- API E2E：通过。
- Frontend Smoke E2E：通过。
- Frontend Browser E2E：通过。
- Customer UI workflow E2E：失败。

失败点：

- 客户通过 UI 新增成功，后端返回新客户 ID。
- 客户列表分页接口按新客户名称查询也能返回记录。
- 前端列表和详情页会按 `@MaskField(NAME)` 对客户姓名脱敏展示，原脚本等待完整客户名称精确可见，导致误判超时。

已追加修复：

- `frontend/scripts/customer-workflow-e2e.mjs` 不再用完整敏感姓名作为 UI 唯一断言。
- 搜索命中以分页接口返回的客户 ID 为准。
- 页面行定位优先使用非敏感客户编号，缺失时回退到脱敏姓名。
- 详情页校验使用客户编号或脱敏姓名，同时继续校验 URL 中客户 ID 与创建响应一致。

本次修复保持生产脱敏策略不变，只让 E2E 断言适配真实页面展示。

GitHub Actions 第三轮中，脱敏断言已通过，Customer UI workflow 继续暴露客户画像雷达图在无头浏览器中的渲染时序问题：

- 客户详情页进入成功，客户 ID 校验成功。
- 客户关系图谱 API 返回客户节点，关系图谱标签页可见。
- 画像雷达图区域在截图中为空，脚本未发现有效图表节点。
- ECharts 雷达轴可读性 warning 被当作运行时问题记录。

已追加修复：

- `CustomerDetail.vue` 在初始化雷达图前等待图表容器真实挂载并具备非 0 宽高。
- 客户详情 loading 结束后再次渲染雷达图，避免在遮罩或布局未稳定时初始化 ECharts。
- Customer UI E2E 对雷达图断言改为检查 `canvas` 或 `svg` 图表节点是否真实渲染。
- 忽略 ECharts 针对雷达轴刻度可读性的非业务 warning。

GitHub Actions 第四轮中，Customer UI workflow 已通过，继续暴露 Screening UI workflow 的大整数 ID 精度问题：

- 测试客户创建成功，后端返回客户 ID `2069016318360563713`。
- 名单筛查页面将输入框客户 ID 转成 JavaScript `Number` 后提交，实际请求变成 `2069016318360563700`。
- 后端按错误 ID 查询客户，返回 `客户不存在`。

已追加修复：

- `ScreeningView.vue` 单笔筛查、批量筛查、白名单新增不再对客户 ID 和名单项 ID 执行 `Number(...)`。
- 新增正整数字符串校验，提交时保留字符串，由后端按 `Long` 反序列化。
- 避免 19 位雪花 ID 在前端发生精度丢失。

GitHub Actions 第五轮中，Customer、Screening、Monitoring UI workflows 已通过，继续暴露 Alert UI workflow 的 Playwright 严格模式问题：

- 预警列表展示人工创建预警正常。
- 预警详情弹窗正常打开。
- 同一预警编号同时出现在基础信息和处置链路节点中，脚本使用精确文本定位且未限定唯一元素，触发 strict mode violation。

已追加修复：

- `alert-workflow-e2e.mjs` 在详情弹窗内验证预警编号时使用 `.first()`，校验“存在即可”，不要求同一业务编号在详情中只出现一次。
- 不改变预警详情页面展示，保留基础信息和处置链路同时展示预警编号的业务表达。
- 后续同一详情弹窗中，`关联交易` 与 `关联交易命中` 同时存在，继续将脚本断言收紧为 `exact + first()`，避免同类 strict mode 误报。
- 预警指派后列表刷新可能改变排序，脚本原先用第一个“处理”按钮，可能点到其他已终态预警的禁用按钮；已改为按当前 `alertNo` 所在表格行定位“查看/指派/处理”按钮。

新增本地验证：

- `node --check frontend/scripts/customer-workflow-e2e.mjs`
- `bash scripts/secret-scan.sh`
- `npm --prefix frontend run test:unit`
- `npm --prefix frontend run build`

GitHub Actions 第六轮中，Customer、Screening、Monitoring、Alert UI workflows 已通过，继续暴露 Case UI workflow 的登录限流污染问题：

- Release Gate 会按独立脚本顺序执行多个浏览器 UI workflow。
- 每个 UI workflow 都会重新登录，且浏览器登录请求默认都来自同一个本机源地址。
- 登录接口本身配置了 5 次/分钟/IP 的防刷限流；当前失败发生在 Case workflow 登录阶段，后端返回登录尝试过于频繁，页面未进入 `/dashboard`。

已追加修复：

- 所有 Playwright UI E2E 脚本支持 `E2E_BROWSER_IP`，并仅对 `/api/**` 请求注入 `X-Forwarded-For` 与 `X-E2E-Run-Id`。
- GitHub Actions 为每个浏览器 UI workflow 分配独立测试 IP，避免不同业务脚本互相消耗登录限流窗口。
- 保留 `frontend-e2e.sh` 的登录限流专项验证，仍由 `E2E_LIMIT_IP` 触发第 6 次 429，不关闭生产限流逻辑。

GitHub Actions 第七轮中，Frontend Browser E2E 验证了隔离 IP 已传入，但全局 `extraHTTPHeaders` 会把 `X-Forwarded-For` 同时带到外部字体请求，导致 Google Fonts CORS 拒绝该非白名单请求头。

已追加修复：

- 移除 Playwright `extraHTTPHeaders` 的全局注入方式。
- 改用 `context.route('**/api/**')` 仅为本项目 API 请求补充 E2E 请求头。
- 保留页面静态资源和外部字体请求的原始请求头，避免非业务 CORS 噪声导致 smoke 回归误判。

GitHub Actions 第八轮中，Frontend Browser、Customer、Screening、Monitoring、Alert UI workflows 已通过，Case UI workflow 进入真实案件闭环后，在案件列表等待完整 E2E 客户名时超时。

原因：

- 案件列表页面会把包含 `E2E` 的历史/测试客户名按 `displayCustomerName` 规则展示为 `客户ID ...`，避免测试名称污染业务列表。
- 脚本仍按完整客户名做精确断言，和页面实际展示不一致。

已追加修复：

- `case-workflow-e2e.mjs` 复刻案件列表客户名称展示规则。
- 案件列表断言改为按 `caseNo`、页面实际展示客户标识和案件摘要组合校验。

GitHub Actions 第九轮中，Case UI workflow 已通过案件列表和案件详情验证，继续暴露调查记录提交失败：

- 后端日志显示 `t_case_investigation.conclusion` 为 `VARCHAR(32)`。
- 脚本的调查结论包含较长中文文本和 `runId`，MySQL 返回 `Data too long for column 'conclusion'`。

已追加修复：

- `case-workflow-e2e.mjs` 将调查结论改为短结论，符合当前数据库字段约束。
- 调查记录提交后先校验接口 HTTP 状态和业务 `code`，再等待页面成功提示；若后端失败，会直接输出接口响应。

GitHub Actions 第十轮中，Case UI workflow 已通过调查记录提交，并进入案件状态流转；连续状态变更时页面同时存在多个 `状态变更成功` 消息，Playwright 严格模式将成功提示定位判为多元素冲突。

已追加修复：

- `case-workflow-e2e.mjs` 对状态变更、调查记录、关闭案件的成功提示统一使用 `.last()` 定位。
- 保留状态标签和后续详情断言作为真实业务结果校验，toast 只作为提交反馈校验。

新增本地验证：

- `node --check frontend/scripts/frontend-browser-e2e.mjs frontend/scripts/*workflow-e2e.mjs`
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`
- `bash scripts/secret-scan.sh`

GitHub Actions 第十一轮中，PR 事件的 Backend Unit And Integration Tests 暴露 `IdGeneratorTest.testUniqueness` 偶发失败：

- 测试期望连续生成 100 个客户编号全部唯一。
- 原实现为“秒级时间戳 + 6 位随机数”，在 CI 快速循环中出现 1 次碰撞，实际唯一值为 99。
- 同一提交的 push 事件后端测试曾通过，说明这是概率性稳定性问题，但业务编号生成不应依赖随机碰运气。

已追加修复：

- `IdGenerator` 改为“毫秒时间戳 + 4 位单调序列”。
- 同毫秒内使用 `AtomicLong` 递增序列，超过序列上限时逻辑时间前推，避免回绕碰撞。
- 保留原有业务前缀和可读时间结构。

新增本地验证：

- `git diff --check`
- `mvn -q -Dtest=IdGeneratorTest test` 因本机缺少 JDK 21 被 Maven Enforcer 拦截，需以 GitHub Actions 后端测试作为该 Java 修复的最终验证。

GitHub Actions 第十二轮中，Backend Unit And Integration Tests 已全部通过，`IdGenerator` 的单调编号修复在 CI 的 JDK 21 环境验证有效。End-To-End Release Gate 继续推进到 Case UI workflow 的最终复查阶段后失败：

- 案件已经完成“创建、调查记录、调查中、待审批、已报送、关闭”全链路。
- 失败点为最终详情复查时等待纯调查内容全文 `exact: true` 出现。
- 页面实际展示会将调查内容渲染为“调查内容：...”或在时间轴中拼接“；结论：...”，并不会单独渲染一段纯调查内容文本。

已追加修复：

- Case UI workflow 最终复查先调用 `/api/cases/{caseId}` 校验本轮调查记录确实沉淀。
- UI 复查改为通过 `/cases?caseId=...` 打开指定案件详情，避免列表排序变化误点其他案件。
- 调查内容页面断言改为包含匹配 `.first()`，保留“案件关闭”时间轴断言。

新增本地验证：

- `node --check frontend/scripts/case-workflow-e2e.mjs`
- `git diff --check`
- `bash scripts/secret-scan.sh`

GitHub Actions 第十三轮中，Case UI workflow 已通过，Release Gate 推进到 STR report UI workflow 后失败：

- STR 来源客户、预警、案件创建与案件状态流转均成功。
- 新建 STR 报告时后端返回“关联案件不存在”。
- 日志中的 `caseId` 与前一步自动创建案件的真实 ID 不一致，符合 JavaScript 将 19 位 Long 解析为 Number 后发生精度漂移的特征。

已追加修复：

- `str-report-workflow-e2e.mjs` 增加 `parseJsonPreservingLongIds`。
- `authedFetch` 与 `waitForApiJson` 在解析响应前，将 16 位以上的 `id/caseId/reportId/alertId/customerId` 等 ID 字段保留为字符串。
- 后续 STR 创建、审核、报送链路使用精确 ID，避免浏览器脚本在业务主键上发生隐式改值。

新增本地验证：

- `node --check frontend/scripts/str-report-workflow-e2e.mjs`
- `git diff --check`

GitHub Actions 第十四轮中，STR report UI workflow 仍在创建报告阶段失败。进一步定位后确认：

- 后端已全局配置 Long 转 String，脚本拿到的 `caseId` 本身是精确字符串。
- 真正导致精度丢失的是 `StrReportView.vue` 创建表单提交时执行 `Number(createForm.caseId)`。
- 19 位案件 ID 被页面代码转为 JavaScript Number 后再次漂移，提交到后端后自然找不到关联案件。

已追加修复：

- `StrReportView.vue` 创建 STR 报告时不再把 `caseId` 转成 Number。
- 表单提交改为传递 `createForm.caseId.trim()`，由后端按 Long 字段完成类型绑定。

新增本地验证：

- `node --check frontend/scripts/str-report-workflow-e2e.mjs`
- `npm --prefix frontend run build`
- `git diff --check`

## 结论

本次集成已保留两侧核心成果，远程 Release Gate 已修复浏览器下载稳定性、脱敏断言、大整数 ID、预警流程严格定位、浏览器登录限流隔离、案件流程断言、业务编号随机碰撞、STR 脚本长 ID 精度问题以及 STR 创建表单 Number 转换问题。当前后端、前端构建、容器构建均已在 GitHub Actions 通过，仍需等待最新 STR 页面修复后的完整 End-To-End Release Gate。

后续建议：

1. 推送最新 STR 页面 `caseId` 提交修复到 PR 分支。
2. 等待 GitHub Actions 重新执行完整 Release Gate。
3. 如 CI 全绿，再将该集成分支合入 `main`。
4. 合入 `main` 后，再安排完整服务栈 E2E 与人工业务回归。
